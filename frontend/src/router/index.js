import { createRouter, createWebHistory } from 'vue-router'
import RobotaHeader from '../components/RobotaHeader.vue'

const router = createRouter({
  history: createWebHistory(import.meta.env.BASE_URL),
  routes: [
    {
    },
  ]
})

export default router
